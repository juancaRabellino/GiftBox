const initialState={
    todosLosPaquetes:[]
}
const paqueteReducer=(state=initialState,action)=>{

    switch (action.type) {
          
        default:
            return state;
    }

}
export default paqueteReducer;